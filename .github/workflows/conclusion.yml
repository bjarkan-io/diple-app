name: Slack Build Conclusion
on: [check_run, check_suite]

jobs:
  slack_build_fail:
    #if: github.event.conclusion == 'failure'
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo ${{github.event}}
      - name: Slack Notification
        uses: rtCamp/action-slack-notify@master
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_USERNAME: GitHub Actions
          SLACK_ICON: https://raw.githubusercontent.com/github/explore/2c7e603b797535e5ad8b4beb575ab3b7354666e1/topics/actions/actions.png
          SLACK_TITLE: Build failed 🤯

  slack_build_success:
    #if: github.event.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo ${{github.event}}
      - name: Slack Notification
        uses: rtCamp/action-slack-notify@master
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_USERNAME: GitHub Actions
          SLACK_ICON: https://raw.githubusercontent.com/github/explore/2c7e603b797535e5ad8b4beb575ab3b7354666e1/topics/actions/actions.png
          SLACK_TITLE: Build failed 🤯
