/// The configuration of the current build
enum Environment {
  /// For development purposes; connects to the [development] environment
  dev,

  /// For production; connects to the [production] environment
  prod,
}
